{
  "name": "primetrade-backend",
  "version": "1.0.0",
  "description": "Backend for Primetrade AI Assignment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check routes/authRoutes.js && node --check routes/taskRoutes.js && node --check routes/healthRoutes.js && node --check controllers/authController.js && node --check controllers/taskController.js && node --check controllers/healthController.js && node --check middleware/auth.js && node --check middleware/error.js && node --check middleware/notFound.js && node --check middleware/validate.js && node --check middleware/validators.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.1",
    "mongodb-memory-server": "^11.0.1",
    "mongoose": "^7.4.1",
    "morgan": "^1.10.0",
    "node-cache": "^5.1.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
