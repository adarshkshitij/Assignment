version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: primetrade_mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=primetrade_tasks
    restart: always

volumes:
  mongodb_data:
